


function fo(msg:string){

}





const names = ['11',2222,'33','44']

// item 可以不加类型注解   会自动推导

// item根据上下文的环境推导出来的，这个时候可以不添加 类型注解
// 上下文中的函数 可以不添加类型注解
names.forEach((item) => {
    console.log('item',item)
})



export {

}